{
  "name": "germanarias-website",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/assets && cp ./*.html ./CNAME dist/ && cp -R ./assets/. dist/assets/",
    "deploy": "npm run build && wrangler deploy && npm run db:migrate:remote",
    "deploy:check": "npm run build && wrangler deploy --dry-run",
    "db:migrate:local": "wrangler d1 migrations apply germanarias-content --local",
    "db:migrate:remote": "wrangler d1 migrations apply germanarias-content --remote"
  },
  "devDependencies": {
    "wrangler": "^4.127.0"
  }
}
